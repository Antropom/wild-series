{% extends 'base.html.twig' %}

{% block title %}Edit Program{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-10">
                <h1>Edit Program</h1>

                {{ include('program/_form.html.twig', {'button_label': 'Update'}) }}

                <button class="btn btn-success mt-2" type="button">
                <a class="text-white" href="{{ path('season_index') }}">back to list</a>
                </button>

                {{ include('program/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
