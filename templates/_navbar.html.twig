<nav class="navbar navbar-expand-lg navbar-light bg-success">
    <a class="navbar-brand" href="{{ path('app_index') }}">Wild Series</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('program_index') }}">Series
                    <span class="sr-only">(current)</span>
                </a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('category_index') }}">Catégories
                    <span class="sr-only">(current)</span>
                </a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('season_index') }}">Saisons
                    <span class="sr-only">(current)</span>
                </a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('episode_index') }}">Épisodes
                    <span class="sr-only">(current)</span>
                </a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('actor_index') }}">Acteurs
                    <span class="sr-only">(current)</span>
                </a>
            </li>
        </ul>
        {% if app.user %}
            <a class="nav-link text-black-50" href="{{ path('app_profile') }}">{{ app.user.email }}</a>
            <a class="nav-link text-black-50" href="{{ path('app_logout') }}"> Se déconnecter</a>
        {% else %}
            <a class="nav-link text-black-50" href="{{ path('app_login') }}"> Se connecter</a>
        {% endif %}
    </div>
</nav>
